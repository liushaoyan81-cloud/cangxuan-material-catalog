# 苍玹材料选型中心上线说明

## 当前状态

- 本地预览：`http://127.0.0.1:4173/`
- 同一局域网预览：`http://192.168.0.2:4173/`
- 手机、平板和电脑均已做响应式检查。
- 发布版只需要 `index.html`、`app.js`、`styles.css`、`docs.css`、`server.mjs` 和 `public/` 下的网页图片。
- `public/catalogs/` 的原始 PDF 不需要上传到公开站点，已在 `.gitignore` 中排除。

## 推荐的公开部署方式

### 方案 A：Cloudflare Pages

适合本项目的静态网页和大量 WebP 图片，支持 HTTPS、CDN、缓存和自定义域名。

- 构建命令：无需构建，直接发布项目目录
- 输出目录：项目根目录
- 自定义域名：在 Cloudflare Pages 项目中添加域名，再按提示配置 DNS

### 方案 B：GitHub Pages

适合先快速获得公开测试网址。需要把本项目发布到一个公开仓库，网址通常为：

`https://账号名.github.io/仓库名/`

绑定自定义域名时，在仓库 Pages 设置中填写域名，并在域名 DNS 中添加平台要求的记录。

## 正式上线前需要确认

1. 选择 Cloudflare Pages 或 GitHub Pages。
2. 确认是否已有域名；没有域名时先使用平台提供的临时网址验收。
3. 确认公开仓库/公开站点可以展示公司介绍和供应链产品图片。
4. 上线后再补充网站备案、隐私说明和联系入口。

项目当前网页资源约 73MB，适合静态 CDN 发布；原始供应商 PDF 保留在本地，不公开上传。
